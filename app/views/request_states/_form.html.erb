<%= form_for(@request_state) do |f| %>
  <% if @request_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request_state.errors.count, "error") %> prohibited this request_state from being saved:</h2>

      <ul>
      <% @request_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
