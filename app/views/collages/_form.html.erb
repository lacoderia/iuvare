<%= form_for(@collage) do |f| %>
  <% if @collage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@collage.errors.count, "error") %> prohibited this collage from being saved:</h2>

      <ul>
      <% @collage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :order %><br>
    <%= f.number_field :order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
